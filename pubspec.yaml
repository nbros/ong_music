name: flutter_ong_log
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  csv: ^5.1.1
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.7
  http: ^0.13.6
  logger: ^2.0.2+1
  logging: ^1.2.0
  path: ^1.8.2
  sqflite: ^2.2.8+4
  sqflite_common_ffi: ^2.2.5
  url_launcher: ^6.2.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
